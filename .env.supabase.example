# Supabase Configuration Example
# Copy this to .env and replace placeholders with your actual values

# Database Connection (from Settings → Database → Connection string)
# Use Session pooler (port 5432) for web apps
# Note: URL-encode special characters in password: @ → %40, / → %2F, ! → %21
DATABASE_URL=postgresql://postgres.[YOUR-PROJECT-REF]:[YOUR-PASSWORD]@[YOUR-HOST]:5432/postgres

# Supabase API Keys (from Settings → API)
SUPABASE_URL=https://[YOUR-PROJECT-REF].supabase.co
SUPABASE_ANON_KEY=[YOUR-ANON-PUBLIC-KEY]
SUPABASE_SERVICE_KEY=[YOUR-SERVICE-ROLE-SECRET-KEY]

# Keep your existing settings
COMFYUI_URL=https://jfmkqw45px5o3x-8188.proxy.runpod.net
# ... other settings
