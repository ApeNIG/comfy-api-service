# Environment
ENVIRONMENT=development
DEBUG=true
FRONTEND_URL=http://localhost:8001

# Database (SQLite for quick start)
DATABASE_URL=sqlite:///./creator_dev.db

# Redis
REDIS_URL=redis://localhost:6379/0

# Security (development keys - CHANGE IN PRODUCTION!)
SECRET_KEY=dev-secret-key-for-jwt-signing-change-in-production
ENCRYPTION_KEY=dev-encryption-key-for-tokens-change-in-production

# Google OAuth (placeholder - replace with real credentials)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REDIRECT_URI=http://localhost:8001/auth/google/callback

# Stripe (placeholder - replace with real credentials)
STRIPE_PUBLIC_KEY=pk_test_placeholder
STRIPE_SECRET_KEY=sk_test_placeholder
STRIPE_WEBHOOK_SECRET=whsec_placeholder

# ComfyUI
COMFYUI_URL=http://localhost:8188

# Email (optional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=console
